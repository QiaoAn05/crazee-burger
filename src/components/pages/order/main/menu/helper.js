export const checkIfProductIsClicked = (
  idProductInMenu,
  idProductClickedOn
) => {
  return idProductInMenu === idProductClickedOn;
};

//helper à côté du composant qu'il doit aider pour désengorger du code
//aide à la lisibilité
//ici il help le Menu.jsx
